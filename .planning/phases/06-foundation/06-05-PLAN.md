---
phase: 06-foundation
plan: 05
type: execute
wave: 4
depends_on: ["06-04"]
files_modified:
  - Makefile
  - go.mod
  - go.sum
autonomous: false
requirements:
  - FOUND-03
  - FOUND-05
  - FOUND-01

must_haves:
  truths:
    - "web/ directory does not exist in the repository"
    - "internal/api/ directory does not exist in the repository"
    - "package.json does not exist in the repository"
    - "go build succeeds without npm, Node.js, or templ CLI installed"
    - "All _templ.go files are committed and tracked by git"
    - "Makefile build target does not depend on web-build"
  artifacts:
    - path: "Makefile"
      provides: "Updated build targets without npm dependency"
      contains: "build-go"
  key_links:
    - from: "Makefile"
      to: "go build"
      via: "build target no longer calls web-build"
      pattern: "^build:"
---

<objective>
Delete the React SPA and JSON API artifacts, update the Makefile, and verify the single-binary build works cleanly.

Purpose: Complete the migration from React SPA to Templ + HTMX by removing all old artifacts. This is the final housekeeping step that makes the repository clean — no npm, no Node.js, no JSON API, just Go.

Output: Clean repository with only the Templ + HTMX web UI, all _templ.go files committed, Makefile updated, go build works without any frontend toolchain.
</objective>

<execution_context>
@/Users/ryanstern/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanstern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-foundation/06-CONTEXT.md
@.planning/phases/06-foundation/06-RESEARCH.md
@.planning/phases/06-foundation/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete React SPA, JSON API, and update Makefile</name>
  <files>Makefile</files>
  <action>
    1. Delete `web/` directory entirely: `rm -rf web/` (React SPA, package.json, node_modules, Vite config, tsconfig)
    2. Delete `internal/api/` directory entirely: `rm -rf internal/api/` (server.go, handlers.go, middleware.go, tests)
    3. Delete `internal/web/dist/` if it still exists (old Vite output embedded by previous embed.go)
    4. Delete `config.toml` from root if it's a stale artifact (check if it's tracked or generated)
    5. Verify no other files reference `internal/api` package:
       - Check for import paths in all .go files: `grep -r '"github.com/wesm/msgvault/internal/api"' --include='*.go'`
       - If any imports found (e.g., in cmd/ files): remove those imports and update the code
       - Check if there's a `cmd/msgvault/cmd/serve.go` or similar that starts the API server — if so, remove that command registration
    6. Update `Makefile`:
       - Change `build` target: remove `web-build` dependency. New target:
         ```makefile
         build:
         	CGO_ENABLED=1 go build -tags fts5 -ldflags="$(LDFLAGS)" -o msgvault ./cmd/msgvault
         	@chmod +x msgvault
         ```
       - Remove `web-install`, `web-dev`, `web-build` targets entirely
       - Keep `build-go` target as an alias (or remove if redundant — `build` now IS `build-go`)
       - Add `templ-generate` target (dev-only, not part of build):
         ```makefile
         templ-generate:
         	go run github.com/a-h/templ/cmd/templ@v0.3.1001 generate
         ```
       - Update `clean` target: remove `rm -rf internal/web/dist` line, keep `rm -f msgvault`
       - Keep all test, lint, fmt targets as-is
    7. Run `go mod tidy` to clean up any unused dependencies from the removed packages
    8. Ensure ALL `_templ.go` files are generated and up to date:
       - Run `go run github.com/a-h/templ/cmd/templ@v0.3.1001 generate`
       - Verify all `.templ` files have corresponding `_templ.go` files
    9. Run full build verification:
       - `go fmt ./...`
       - `go vet ./internal/web/...`
       - `go build -tags fts5 ./cmd/msgvault`
       - `go test -tags fts5 ./...` (full test suite — some tests in internal/api will be gone, which is expected)
    10. Verify `_templ.go` files are tracked by git (not gitignored):
        - Check `.gitignore` for patterns that might exclude them
        - If `.gitignore` has `*_templ.go` or similar, remove that pattern
  </action>
  <verify>
    <automated>cd /Users/ryanstern/msgvault && test ! -d web && test ! -d internal/api && go build -tags fts5 -o /dev/null ./cmd/msgvault && echo "CLEAN BUILD OK"</automated>
  </verify>
  <done>
    - web/ directory deleted
    - internal/api/ directory deleted
    - No references to internal/api package in codebase
    - Makefile build target does not depend on npm/web-build
    - templ-generate target added to Makefile
    - go mod tidy completed (unused deps removed)
    - go build works without npm or templ CLI
    - All _templ.go files generated and present
    - Full test suite passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end browser verification of complete web UI</name>
  <files></files>
  <action>
    User verifies the complete Templ + HTMX web UI in a browser. Claude has already built the binary and removed all React/API artifacts. This checkpoint confirms visual correctness, interactive behavior, and build purity.

    What was built: Complete Templ + HTMX web UI replacing the React SPA. Single-binary build with no npm dependency. All pages: Dashboard, Messages, Aggregate (7 views), Search, Deletions. Keyboard shortcuts, account filtering, deletion staging with OOB badge updates.

    How to verify:
    1. Build and start: `make build && ./msgvault web --port 8484`
    2. Dashboard (http://localhost:8484/) — verify stat cards show real data, top 5 senders/domains
    3. Messages (http://localhost:8484/messages) — verify paginated list, click sort headers, click message for detail
    4. Aggregate (http://localhost:8484/aggregate) — click through all 7 tabs, click row to drill down, verify breadcrumbs
    5. Search (http://localhost:8484/search) — type query, verify debounced results
    6. Deletions (http://localhost:8484/deletions) — stage from aggregate drill-down, verify badge updates, cancel
    7. Keyboard shortcuts — j/k rows, Enter open, Esc back, / search, ? help, Tab cycle views
    8. Account filter — select account in dropdown, verify all pages filter
    9. Binary purity — `make clean && make build` with no npm errors
  </action>
  <verify>User confirms all pages work correctly in browser with real data</verify>
  <done>User types "approved" confirming all verification steps pass, or describes specific issues to fix</done>
</task>

</tasks>

<verification>
- `test ! -d web` — web directory gone
- `test ! -d internal/api` — API directory gone
- `go build -tags fts5 ./cmd/msgvault` — binary builds
- `go test -tags fts5 ./...` — all tests pass
- `make build` — no npm errors
- `ls internal/web/templates/*_templ.go` — generated files present
- Manual browser verification of all pages
</verification>

<success_criteria>
The React SPA and JSON API are completely removed. The repository contains only the Templ + HTMX web UI. `go build` produces a single binary that serves all pages. All _templ.go files are committed. The Makefile has no npm dependencies. A human has verified that all pages work correctly in the browser with real data.
</success_criteria>

<output>
After completion, create `.planning/phases/06-foundation/06-05-SUMMARY.md`
</output>
