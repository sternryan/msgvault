---
phase: 10-integration-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/web/handlers_test.go
  - internal/web/handlers_messages.go
  - internal/web/templates/thread.templ
  - internal/web/templates/thread_templ.go
autonomous: true
requirements:
  - RENDER-04
  - POLISH-01
gap_closure: true

must_haves:
  truths:
    - "TestMessageBodyWrapperEndpoint passes with assertions matching Phase 9 unified toolbar"
    - "Thread view lazy-loaded messages use unique IDs (email-body-wrapper-{messageID})"
    - "go test ./internal/web/... passes with zero failures"
  artifacts:
    - path: "internal/web/handlers_test.go"
      provides: "Updated test assertions for Phase 9 toolbar + new wrapperID param test"
      contains: "email-toolbar"
    - path: "internal/web/handlers_messages.go"
      provides: "wrapperID query param support in messageBodyWrapper"
      contains: "wrapperID"
    - path: "internal/web/templates/thread.templ"
      provides: "Unique wrapperID in thread lazy-load hx-get URLs"
      contains: "wrapperID=email-body-wrapper-"
  key_links:
    - from: "internal/web/templates/thread.templ"
      to: "internal/web/handlers_messages.go"
      via: "?wrapperID=email-body-wrapper-{msgID} query param in hx-get URL"
      pattern: "wrapperID=email-body-wrapper-"
    - from: "internal/web/handlers_test.go"
      to: "internal/web/handlers_messages.go"
      via: "HTTP test assertions matching handler output"
      pattern: "email-toolbar.*closest .email-render-wrapper"
---

<objective>
Fix two cross-phase integration issues from the v1.1 milestone audit: (1) stale test assertions in TestMessageBodyWrapperEndpoint that reference Phase 7 banner markup instead of Phase 9's unified toolbar, and (2) duplicate `id="email-body-wrapper"` in thread lazy-loaded messages.

Purpose: Close INT-01 (HIGH) and INT-02 (LOW) identified in the v1.1 milestone audit so `go test ./internal/web/...` passes green and thread DOM is spec-compliant.
Output: Updated test file, handler with wrapperID support, thread template with scoped lazy-load IDs, regenerated templ output.
</objective>

<execution_context>
@/Users/ryanstern/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanstern/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-integration-cleanup/10-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From internal/web/handlers_messages.go (messageBodyWrapper):
- Reads `format`, `showImages` query params
- `hasBothFormats := msg.BodyText != "" && msg.BodyHTML != ""`
- When hasBothFormats=true, emits `<div class="email-toolbar">` (NOT `email-images-banner`)
- All hx-target values use `closest .email-render-wrapper` (NOT `#email-body-wrapper`)
- Wrapper div: `<div id="email-body-wrapper" class="email-render-wrapper">`
- 6 fmt.Fprintf calls emit the wrapper ID — all must be parameterized

From internal/web/handlers_test.go (mockEngine.GetMessage):
- Returns both BodyText and BodyHTML non-empty, triggering hasBothFormats=true
- setupTestServer() creates httptest.Server with mockEngine + deletion.NewManager

From internal/web/templates/thread.templ:
- Eager (latest/highlighted): `id={ fmt.Sprintf("email-body-wrapper-%d", msg.ID) }` (line 114)
- Lazy (collapsed): `hx-get={ fmt.Sprintf("/messages/%d/body-wrapper", msg.ID) }` (line 161)
- Lazy uses `hx-swap="innerHTML"` into `div.thread-message-body`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix stale test assertions and add wrapperID param test (INT-01 + INT-02 test)</name>
  <files>internal/web/handlers_test.go</files>
  <action>
In `TestMessageBodyWrapperEndpoint` (line 451-484), update two stale assertions:

1. Replace `strings.Contains(bodyStr, "email-images-banner")` with `strings.Contains(bodyStr, "email-toolbar")` — Phase 9 unified toolbar absorbs the banner when hasBothFormats=true.

2. Replace `strings.Contains(bodyStr, 'hx-target="#email-body-wrapper"')` with `strings.Contains(bodyStr, 'hx-target="closest .email-render-wrapper"')` — Phase 9 changed all hx-target to use class selector.

Keep the three passing assertions unchanged (id="email-body-wrapper", hx-get, hx-swap="outerHTML").

Then add a new test function `TestBodyWrapperWithWrapperIDParam` after `TestMessageBodyWrapperShowImages`:
- GET `/messages/1/body-wrapper?wrapperID=email-body-wrapper-99`
- Assert response status 200
- Assert response contains `id="email-body-wrapper-99"` (the custom ID)
- Assert response does NOT contain `id="email-body-wrapper"` as a standalone attribute (it should be scoped)
- Assert response contains `email-toolbar` (hasBothFormats=true path)

Run `go test ./internal/web/... -run TestMessageBodyWrapperEndpoint -v` — expect PASS after assertion fix.
Run `go test ./internal/web/... -run TestBodyWrapperWithWrapperIDParam -v` — expect FAIL (handler doesn't read wrapperID yet; this is the RED step for Task 2).
  </action>
  <verify>
    <automated>cd /Users/ryanstern/msgvault && go test ./internal/web/... -run TestMessageBodyWrapperEndpoint -v</automated>
  </verify>
  <done>TestMessageBodyWrapperEndpoint passes with updated assertions. TestBodyWrapperWithWrapperIDParam exists (will fail until Task 2 implements handler support).</done>
</task>

<task type="auto">
  <name>Task 2: Add wrapperID param to handler + thread template lazy-load (INT-02 fix)</name>
  <files>internal/web/handlers_messages.go, internal/web/templates/thread.templ, internal/web/templates/thread_templ.go</files>
  <action>
**Handler change (handlers_messages.go):**

At the top of `messageBodyWrapper` (after parsing `showImages`), read optional `wrapperID` query param:
```go
wrapperID := r.URL.Query().Get("wrapperID")
if wrapperID == "" {
    wrapperID = "email-body-wrapper"
}
```

Replace ALL 6 hardcoded `id="email-body-wrapper"` occurrences in the handler's fmt.Fprintf calls with `fmt.Sprintf('id="%s"', wrapperID)`. The 6 locations are:
1. Text view (format=="text") — line ~130: `id="email-body-wrapper"`
2. HTML + showImages + hasBothFormats — line ~178: `id="email-body-wrapper"`
3. HTML + !showImages + hasBothFormats — line ~202: `id="email-body-wrapper"`
4. HTML + !showImages + !hasBothFormats (banner path) — line ~214: `id="email-body-wrapper"`
5. Fallback text-only — line ~232: `id="email-body-wrapper"`
6. Fallback no-body — line ~239: `id="email-body-wrapper"`

Do NOT change any hx-target values (they already use `closest .email-render-wrapper`).
Do NOT change any other handler logic.

**Thread template change (thread.templ):**

In the `else` branch of `ThreadMessageCard` (the collapsed/lazy-load path, line ~159-167), change the hx-get URL from:
```
hx-get={ fmt.Sprintf("/messages/%d/body-wrapper", msg.ID) }
```
to:
```
hx-get={ fmt.Sprintf("/messages/%d/body-wrapper?wrapperID=email-body-wrapper-%d", msg.ID, msg.ID) }
```

This passes the message-scoped ID to the handler, matching the pattern already used by the eager-rendered card (`id="email-body-wrapper-{msgID}"`).

**Regenerate templ output:**

Run `templ generate ./internal/web/templates/` to regenerate `thread_templ.go` from the updated `thread.templ`.

Run `go fmt ./...` and `go vet ./...` per project conventions.
  </action>
  <verify>
    <automated>cd /Users/ryanstern/msgvault && go test ./internal/web/... -v</automated>
  </verify>
  <done>All tests pass including TestBodyWrapperWithWrapperIDParam. Thread lazy-load URLs include ?wrapperID=email-body-wrapper-{msgID}. Handler emits the parameterized ID. No duplicate IDs in thread DOM.</done>
</task>

</tasks>

<verification>
1. `go test ./internal/web/... -v` — all tests pass, zero failures
2. `go vet ./internal/web/...` — no warnings
3. `grep -c 'id="email-body-wrapper"' internal/web/handlers_messages.go` returns 0 (all hardcoded occurrences replaced with parameterized version)
4. `grep 'wrapperID=email-body-wrapper-' internal/web/templates/thread.templ` — lazy-load URLs include scoped ID
</verification>

<success_criteria>
- TestMessageBodyWrapperEndpoint passes with Phase 9 unified toolbar assertions
- TestBodyWrapperWithWrapperIDParam passes validating unique wrapper IDs
- TestMessageBodyWrapperShowImages continues to pass (regression check)
- Full `go test ./internal/web/...` suite passes with zero failures
- Thread lazy-load hx-get URLs pass ?wrapperID=email-body-wrapper-{msgID} to handler
- Handler reads wrapperID param and uses it in all output paths
</success_criteria>

<output>
After completion, create `.planning/phases/10-integration-cleanup/10-01-SUMMARY.md`
</output>
